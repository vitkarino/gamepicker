export class GamePickerUI {
    constructor() {
        console.log("GamePickerUI initialized");
    }

    public searchUser(query: string): void {
        console.log(`Searching for user with query: ${query}`);
    }
}